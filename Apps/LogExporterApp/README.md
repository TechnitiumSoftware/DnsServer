# Log Exporter App

A DNS App for Technitium DNS Server that exports log entries using configurable export strategies.

## Configuration

The app is configured using `dnsApp.config` (JSON).

> This README is intentionally minimal. Refer to the shipped `dnsApp.config` in this folder for the supported configuration keys.
