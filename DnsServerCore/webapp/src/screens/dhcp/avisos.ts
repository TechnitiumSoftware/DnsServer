/* The alert and the translation of a failure live in `lib/aviso`: eight screens
   each had their own. */
export type { Notice } from '../../lib/aviso'
export { noticeFromFailure as errorNotice } from '../../lib/aviso'
