/* The alert and the translation of a failure live in `lib/aviso`: eight screens
   each had their own. */
export type { Notice } from '../../lib/notice'
export { noticeFromFailure as errorNotice } from '../../lib/notice'
